[package]
name = "{{project-name}}"
version = "0.1.0"
authors = ["{{authors}}"]

[lib]
name = "{{crate_name}}"
crate-type = ["cdylib"]

[dependencies]
samp-sdk = {git = "https://github.com/ZOTTCE/samp-sdk"}
